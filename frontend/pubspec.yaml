name: policy_iq
description: PolicyIQ — Advanced Policy Analysis and Intelligence Dashboard for data-driven decision making and policy simulation
publish_to: 'none'
version: 1.0.0+1

# App metadata
homepage: https://policyiq.example.com
repository: https://github.com/policyiq/policyiq
issue_tracker: https://github.com/policyiq/policyiq/issues
documentation: https://docs.policyiq.example.com

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  window_size: ^0.1.0
  http: ^1.1.0
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
